# Mirror Memory - Environment Configuration
# Copy to .env and fill in values for your environment.
# NEVER commit .env with real credentials.

# Database - test environment (Docker Compose default)
DATABASE_URL=postgresql+psycopg://mirror_test:mirror_test_pass@localhost:5433/mirror_memory_test

# Environment marker - prevents accidental production operations
MIRROR_ENV=test
