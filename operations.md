# 内部测试与持续运行手册

适用：已实现并通过对应契约的构建。当前是拟议流程，管理动作须由 T11 工具落实后才能执行。真实命令登记在 [templates](templates.md) 的命令表；未登记时不猜 CLI，不手工改库跳过不变量。

## 1. 首次启动检查

1. 核对环境名、数据库地址/标记、运行代码与 schema 版本；禁止打印密码或完整连接串。
2. 检查业务授权 provider、目的地配置、时钟、预算账本和保留 profile。缺失时只允许明确的合成离线模式。
3. 用健康检查验证数据库、migration、任务领取和删除处理能力。业务进程存活不等于 worker 健康。
4. 用合成用户执行 observe→ready→recall→forget，确认审计无原文。
5. 开启 shadow 或测试名单，登记启动时间、负责人、版本和停止条件。

正常 shutdown：停止接收新测试流量，停止领取新模型任务，等待有界在途任务结束；超时任务留下状态与预算预留，后续恢复核对。不能直接把全部 running 标成完成。

## 2. 日常、周度与月度

| 周期 | 按顺序检查 | 异常动作 | 必留记录 |
| --- | --- | --- | --- |
| 每次批次开始 | 授权、截止日期、预算、配置、数据集与测试名单 | 条件缺失仅跑合成离线测试 | 批次卡 |
| 每个测试工作日 | 撤销同步延迟→删除逾期→最老任务→失败分类→已花费+预留→模型用量对账 | 先解决权限/删除异常，再处理积压和费用 | 巡检卡及异常链接 |
| 每周/每轮结束 | 按类别抽查错误、比较 profile、检查期限和备份、核对失败任务 | 提出有证据的配置变更；保留旧版本 | 质量/成本小结 |
| 每次发布 | 强制契约、留出质量、migration 兼容、回退、环境/名单 | 有阻断失败则不发布 | 发布卡 |
| 每月（持续运行） | 在隔离环境恢复备份、重放删除；检查权限、保留执行、容量趋势 | 不符合恢复要求时暂停扩大试点 | 演练报告 |
| 每个批次结束 | 停止派发、冻结结果、处理在途、启动期限清理、核对副本 | 未清理项进入删除跟踪，不标结项 | 结项/清理回执 |

无人值守运行前，告警必须能通知实际负责人；不以第二天人工巡检替代授权/删除异常的及时处理。监控记录匿名化/不透明 ID、错误码、耗时和用量，不默认采集对话、记忆文本或完整 Prompt。

## 3. 关键故障处置

### O01：可能串用户、越权或撤销失效

1. 隔离受影响 app 的记忆读写与模型外发，Psych 回到 off/原有路径；保留撤销和删除能力。
2. 保存请求 ID、授权版本、时间和代码版本；不要为排错导出全部用户原文。
3. 核对业务变更时间、Mirror ack、dispatch 和提交许可点，区分传播延迟与真正越权提交。
4. 通知业务负责人评估已经交付或外发的范围；Mirror 无法追回已被业务/供应商接收的数据。
5. 修复后重跑 C01–C04、C12；只在确认边界恢复后重新启用测试名单。

### O02：worker 卡住、队列积压或模型连续失败

1. 看最老任务、租约与 heartbeat、失败类型、供应商错误、数据库可用性；先判断资源不足还是永久输入错误。
2. 暂停新的昂贵工作或进入 constrained；满队列拒绝新事件并明确 retryable，不能 accepted 后丢弃。
3. 等租约恢复或通过受控工具领取；仅按 job/run ID 重放可重试任务，保持原去重键。
4. 永久 schema/权限错误不重复调用模型；旧授权任务取消，保留到期逻辑继续运行。
5. 用一小批合成事件确认恢复，再逐渐解除限制；检查未知调用费用。

### O03：费用异常或护栏频繁开关

1. 暂停新模型 dispatch，保留合法已有记忆读取及删除能力。
2. 对账已结算、在途预留、超时重试、shadow 和评测 judge；不要通过清空账本恢复服务。
3. 检查是否重复提交/计费、模型价格或配置变化；必要时回退到已验证 profile。
4. 确认冷却、恢复阈值和剩余预算后有限恢复；不能自动把预算上限调高。
5. 单独记录费用估算误差，后续用量上限采用保守余量。

### O04：记忆错误或陈旧内容影响回复

1. 定位实际使用的 memory ID/revision 和 receipt；确认错误来自提取、整合、检索还是回答模型。
2. 通过 correct 停用可定位的错误项；广泛问题关闭该候选域的 active_internal，保留 shadow。
3. 回退 Prompt/规则仅影响后续生成，不能自动消除已有坏视图；明确使受影响修订失效并按仍有效证据重建。
4. 重跑相应 Q 类及纠正/历史查询契约；无可靠来源的旧结果不恢复。

### O05：删除逾期或数据复活

1. 确认阻断屏障仍在；若已失效，立即隔离该 scope 读写与外发。
2. 查看主库、视图、任务、投影、导出文件和日志副本的清理进度；备份单独跟踪。
3. 对失败清理任务限定范围重试；不能仅删除主表就标 verified。
4. 检查是否历史重灌换新 ID、旧 worker 提交或备份恢复未重放；将案例加入 C12/C13。
5. 只有实际验证过的存储标完成；外部供应商状态不明时如实记录。

### O06：数据库不可用或授权 provider 不可达

停止依赖当前权限的读写与模型派发；调用方可无 Mirror 上下文继续正常对话，但不能返回未复核的旧记忆缓存。恢复数据库后核对授权版本、删除屏障、租约和预算，再恢复任务；恢复 provider 后先同步最新版本，不能默认把过期许可续上。

## 4. 版本与配置发布

1. 冻结代码、schema、Prompt、domain、模型和 profile 版本；不要用一个“v2”笼统代替所有版本。
2. 在隔离环境运行受影响契约及质量回归；记录 migration 的前置条件、执行时间和能否回退。
3. 拍取符合保留规则的备份，验证恢复所需删除账本在备份之外也可取得。
4. 首先 shadow，再按内部名单开启；观察费用、错误与质量，满足发布卡的窗口后扩大。
5. 超过停止条件回退流量/配置；schema 破坏性变更不能假设降级应用即恢复。必要时停止写入并按已演练的前向修复或恢复方案处理。

支持回退不意味着恢复被删数据。数据库恢复须在关闭入口的环境进行：恢复→应用 schema→重放删除/保留到期→同步当前授权→核对任务与费用→重建投影→跑隔离/删除冒烟→开放测试名单。拿不到删除账本或当前授权时保持入口关闭。

## 5. 内部试点保留 profile（候选默认，尚未启用）

以下是为了可评审而提出的内部测试起点，不是行业标准或正式产品承诺。实际批次必须记录采纳/修改结果；执行实现前须通过 C12/C13。无需为纯合成无个人数据的本地演示假装办理真实同意。

| 数据 | 候选默认 | 执行与例外 |
| --- | --- | --- |
| 真实测试原文及派生记忆 | 最早到期日为：采集后 30 天，或批次结束后 7 天 | 批次必须先填明确结束日；来源到期连带失效；延长需重新确认用途和授权，不默认续期 |
| 不含内容的运行诊断日志 | 14 天 | 仍控制标识与访问；含敏感内容的误日志按事件清理，不以 14 天为借口等待 |
| 临时导出文件 | 生成后 24 小时 | 下载前验权；用户已下载副本不在 Mirror 可清理范围内 |
| 内部测试备份 | 7 天滚动窗口 | 只由明确任务产生，登记位置与清理；恢复必须重放删除 |
| 在线删除副本 | 阻断同步确认；候选清理目标 24 小时内 | 目标未达告警，回执保持未完成；不是“允许继续召回 24 小时” |
| 删除防重放最小标记 | 直到相关任务、历史输入和备份窗口全部失效 | 不含原文；按清理证明终止保留，不能与内容一起提前删除 |

现有 Psych 数据仍遵循其业务策略；本 profile 仅适用于新的 Mirror 内部测试副本。供应商留存单独登记，不声称数据库 TTL 能删除供应商副本。长期遗忘先用合成数据和模拟时钟检验，不能为测试长时间跨度默认保留真实敏感原文。

## 6. 批次/产品退出

1. 停止新事件派发，关闭测试名单，撤销该批次记忆处理权限。
2. 等待或取消在途任务，处理未知模型调用和费用结算。
3. 通过明确 selector 删除测试数据，核对主库、投影、导出和监控副本；保留最小清理凭据。
4. 等备份窗口结束并核验，不提前删除恢复所需的删除标记。
5. 撤销不再需要的测试凭据和访问，保留无个人内容的结果报告与代码版本。

旧 Mirror 仓库退役仅在确定仓库/服务资源清单后执行，不以“无需兼容”为由删除 Psych 或未知资源。归档代码不等于停服务，停服务也不等于清数据，三者分别登记。
